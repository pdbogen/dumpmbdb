dumpmbdb
========

A simple perl script to dumb the MBDB files that are included with iPhone backups in a (mostly) human-readable format. Includes bonus SHA-1 calculation of the file name for the corresponding file in the backup.